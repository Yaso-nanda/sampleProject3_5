pipeline {
     agent any
     
     stages {
         stage('testing'){
           steps{
             withCredentials([string(credentialsId: 'nexus_pass', Variable: 'PASSWORD')]) {
                sh 'echo $PASSWORD'
             }
         }
         }
     }
}
